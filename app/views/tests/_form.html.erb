 <div class= "panel panel-default">
   <div class "panel-body">
 <%= form_for(@test) do |f| %>
  <% if @test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test.errors.count, "error") %> prohibited this testimony from being saved:</h2>

      <ul>
      <% @test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Testimony %><br>
    <%= f.text_area :test, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :Name %><br>
    <%= f.text_field :tester, class: 'form-control' %>
  </div>
  <div class='panel-footer'>
  <div class="actions">
    <%= f.submit "Add your testimony!", class:'btn btn-info' %>
  </div>
  </div>
<% end %>
</div>  
</div> 